# ⭐ Star GIF Creator
![Star GIF](stars.gif)

A simple Python project that creates an animated GIF from images using the `imageio` library.

## What it does

This program reads image files and combines them into an animated GIF.

## Requirements

- Python 3
- imageio

## Installation

Install imageio with:

```bash
pip install imageio